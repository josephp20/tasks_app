apiVersion: v1
kind: Service

metadata:
  name: tasks-service

spec:
  type: NodePort

  selector:
    app: tasks-app

  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080